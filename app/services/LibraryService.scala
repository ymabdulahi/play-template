package services

import javax.inject.{Inject, Singleton}

@Singleton
class LibraryService @Inject()() {// Optional: to separate concerns, move nearly everything that shouldn't be in the controller here

}
